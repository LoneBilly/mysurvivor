import React from 'react';

const GameUI = () => {
  return (
    <div className="text-white p-4">
      <h1 className="text-2xl font-bold">Interface de Jeu</h1>
      <p>Ce composant assemblera le Header, la Grille, le Footer, et les Modales.</p>
      <p>Les données du jeu sont maintenant prêtes à être utilisées depuis `useGame()`.</p>
    </div>
  );
};

export default GameUI;